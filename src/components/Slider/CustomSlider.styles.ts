import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    subtitle: {
        alignItems: 'center',
        fontWeight: 'bold',
        color: "#000000",
        fontSize: 15,
        margin: 15,
        marginTop: 25,
    },
})