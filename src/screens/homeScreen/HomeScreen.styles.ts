import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        backgroundColor: '#ffffff',
        paddingBottom: 60
    },
    title: {
        alignItems: 'center',
        fontWeight: 'bold',
        color: "#000000",
        fontSize: 20,
        margin: 20,
    }
})